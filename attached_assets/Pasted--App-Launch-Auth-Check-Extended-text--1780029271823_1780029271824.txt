 App Launch & Auth Check (Extended)
text
┌─────────────────────────────────────────────────────────────────┐
│                    APP LAUNCH (Expo Router)                     │
│   Providers: AuthContext, CartContext, LanguageContext,          │
│   PlatformConfigContext, QueryClient, AnalyticsProvider,        │
│   OfflineQueueProvider, ErrorBoundary                           │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│     GET /api/platform-config (first call)                       │
│     Timeout: 8s → fallback to cached config (local storage)    │
│     Error: show “Limited functionality” banner if no cache.     │
│     Bootstraps: flags, fees, UI text, maintenance, loyalty,     │
│     analytics settings, crash reporting dsn, force-upgrade ver  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│     Force Upgrade Check: minAppVersion in config               │
│     If current < minAppVersion → ForceUpdateScreen (no bypass) │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│     appStatus check:                                           │
│     "maintenance" → MaintenanceScreen (retry every 30s)        │
│     "limited"     → AnnouncementBar + restricted features       │
│     "active"      → proceed                                     │
│     "down"        → ServerDownScreen (contact support)         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│     AuthGuard.tsx — Check stored token (AsyncStorage)          │
└─────────────────────────────────────────────────────────────────┘
                 │                             │
           TOKEN FOUND                   NO TOKEN
                 │                             │
                 ▼                             ▼
┌──────────────────────────┐     ┌──────────────────────────────┐
│  POST /api/auth/refresh   │     │       app/auth/index.tsx      │
│  (httpOnly cookie/web)    │     │  Login or Guest Browse        │
│  Or try Biometric first   │     │  (restricted features)        │
│  Then validate token       │     └──────────────────────────────┘
│  Connect Socket.IO (with  │
│   auth token)             │
│  Extract loyalty tier     │
└──────────────────────────┘
          │           │
       VALID        INVALID (401/403)
          │           │
          │           ▼
          │    Clear tokens → Login (show expiry message)
          ▼
┌─────────────────────────────────────────────────────────────────┐
│   HOME SCREEN  (tabs/index.tsx)                                 │
│   • Service grid (ServiceGuard with analytics)                  │
│   • Banners (track impressions)                                 │
│   • Flash deals (countdown accuracy)                            │
│   • Recommendations (error fallback: generic popular items)     │
└─────────────────────────────────────────────────────────────────┘