/**
 * useRateLimitCountdown — admin-panel
 *
 * Re-exports the shared hook from @workspace/auth-react.
 * The canonical implementation lives in lib/auth-react/src/hooks/useRateLimitCountdown.ts.
 */
export { useRateLimitCountdown } from "@workspace/auth-react";
export type { RateLimitCountdown } from "@workspace/auth-react";
