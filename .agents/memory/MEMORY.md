- [Admin stale import pattern](admin-missing-routes.md) — admin.ts had BOTH a missing import (./revenue-analytics, non-existent) AND a correct one (./admin/revenue-analytics.js); remove the stale line 1 import + its router.use to unblock startup.
- [Rider internal delegation pattern](rider-aliases.md) — top-level reference-API alias routes delegate to riderRouter by rewriting req.url before calling riderRouter(req,res,next); riderAuth + feature gates all run inside the router unchanged.
- [Rider registration submit bugs](rider-reg-submit-bugs.md) — 8 bugs found+fixed across 2 passes: docs endpoint, camera lock, race condition, silent failure toast, offline queue (password stripped), CNIC inline error, doAutoLogin loading flash, biometric save error misrouted to roleError screen.
- [index.ts stale proxy-config block](index-proxy-config.md) — lines 584-595 of index.ts imported getWsUpgradeHandlers from ./proxy-config (non-existent); the real export is ./app.js and wiring already done at line ~451; safe to remove the duplicate block.
- [WS HMR proxy limitation](ws-hmr-replit.md) — Vite HMR WS through API proxy returns 400; setting hmr.path doubles the prefix; leave as-is (dev only, not functional).
- [Wallet refund integrity](wallet-refund-integrity.md) — failed-ride refunds must sum original `debit` txns (platformFee is NOT additive to fare) and guard status transition for idempotency.
- [Admin fetch path scoping](wallet-refund-integrity.md) — adminFetch() prepends /api/admin, so endpoints must omit the /admin prefix (else doubled /api/admin/admin/... → 404).
- [http-proxy-middleware v3 WS wiring](ws-upgrade-wiring.md) — v3 requires explicit server.on('upgrade', handler) via module-level _wsUpgradeHandlers array exported via getWsUpgradeHandlers().
- [Ride lifecycle E2E verified](ride-lifecycle-e2e.md) — full lifecycle tested: accept→arrived(proximity 500m)→OTP verify→in_transit→complete; dev bypass "0000" added to verify-otp endpoint; cash ride deducts 20% platform fee from wallet.
- [Rider auth E2E & bypass map](rider-auth-e2e.md) — login=send-otp+verify-otp(devCode); refresh=HttpOnly Secure cookie only (body rejected by design); logout=POST /auth/logout (was missing, now in refresh.ts); 5 bypass methods documented.
- [Production deploy setup gate 503](production-deploy-setup-gate.md) — JWT_SECRET absent in prod → setup gate returns 503 on every route → Cloud Run health check fails → promote fails; fix: set all 10 secrets via setEnvVars({ environment:"production" }).
- [esbuild NODE_ENV baking](esbuild-nodeenv-baking.md) — Cloud Run bakes NODE_ENV=production during build causing all secret guards to process.exit(1); fix: hardcode "development" in build.mjs define, never read process.env.NODE_ENV at build time.
- [Artifact workflow port conflict](artifact-workflow-ports.md) — Artifact workflows grab canonical ports (5000) if skip-guard is removed; keep skip-guard in dev scripts + API_PORT=8080 so artifacts exit cleanly and canonical workflows own their ports.
- [Workflow zombie entries](workflow-zombie-limit.md) — configureWorkflow registry can get stuck at 10/10 with zombie entries that can't be deleted; restart the code_execution sandbox (restart:true) to clear stale count view; zombie root cause is broken original import path (cd extracted/12345678).
- [Admin route error handler pattern](admin-route-error-handler.md) — Express 5 auto-propagates async errors; use router.use(4-arg err handler) at the end of each admin router file instead of wrapping every handler with asyncHandler.
- [Security helpers fail-open audit](security-helpers-audit.md) — _isVpnOrProxy returns false on error (fail-closed, circuit breaker mitigates); isIPBlocked has in-memory Set as primary guard; both are acceptable; isAdminIpWhitelisted already returns false on error.
- [Rider toast global migration](rider-toast-migration.md) — use toast({ title, variant }) from @/hooks/use-toast (pages) or ../../hooks/use-toast (components); ajkmart uses its own ToastContext showToast — do NOT change those.
- [Refresh response envelope mismatch](refresh-response-envelope.md) — createApiFetcher.doRefresh and useTokenRefresh.doRefresh both misread the server refresh response; caused immediate logout on session resume.
- [Rider auth circular dep loop](rider-auth-circular-dep.md) — startup useEffect([sharedAuth]) called sharedAuth.login() inside, causing infinite re-run; fix: refs + [retryKey] only. Also: triggerLogout passed reason to callback and skipped dispatchEvent to prevent double clearAuth race. AuthProvider refreshEndpoint default="/api/auth/refresh" doubled with baseURL="/api"; fix: pass refreshEndpoint="/auth/refresh" explicitly.
- [AuthProvider concurrent refresh race](authprovider-startup-race.md) — AuthProvider startup + rider validateToken both POST to /auth/refresh with same cookie; server dedup returns 401 to second caller → logout; fix: disableStartupRefresh={true} prop on SharedAuthProvider in rider-auth.tsx.
- [OTP bypass duplicate routes](otp-bypass-duplicate-routes.md) — system/users.ts had duplicate POST+DELETE /users/:id/otp/bypass handlers mounted BEFORE otp.ts; always shadowed otp.ts comprehensive handlers; fix: remove duplicates from system/users.ts.
- [OTP bypass checkOTPBypass dedup](otp-bypass-dedup.md) — send-otp path called checkOTPBypass 3x per request (middleware+handler+sendOtp); fix: cache in res.locals from otpLimiterOrBypass, read in handler, pass as OtpSendOptions.precomputedBypass to sendOtp().
- [OTP Control refactor pattern](otp-control-refactor.md) — disableOtpGlobally cap raised from 1440m to 5256000m (indefinite); GET /admin/otp/bypasses added; otp-control.tsx rewritten to simple toggle + channels + rate-limits + filtered audit; new /otp-bypass-management page; quick-bypass removed from users.tsx.
